{
  "name": "zebras-draft-console",
  "private": true,
  "type": "module",
  "description": "Browser tests for the live draft console. Build with app/build.py first.",
  "scripts": {
    "build": "python3 app/build.py",
    "serve": "cd app/dist && python3 -m http.server 8777",
    "test": "node app/test/sync.mjs",
    "test:live": "node app/test/live.mjs"
  },
  "devDependencies": {
    "playwright": "^1.49.0"
  }
}
